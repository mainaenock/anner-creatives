"use client";

import Image from "next/image";
import { useMemo, useState } from "react";
import { ArrowRight, Check, Heart, Minus, Package, Plus, Search, ShoppingBag, Sparkles, Trash2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Sheet, SheetContent, SheetDescription, SheetHeader, SheetTitle } from "@/components/ui/sheet";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";

type Product = { id: number; name: string; category: string; price: number; oldPrice?: number; image: string; color: string; description: string };
type CartItem = Product & { quantity: number };
const starterProducts: Product[] = [
  { id: 1, name: "Sage Corduroy Backpack", category: "Backpacks", price: 2850, oldPrice: 3400, image: "/featured-backpack.jpg", color: "#d9e9bb", description: "Soft corduroy, roomy pockets and a playful woven zip charm." },
  { id: 2, name: "Lavender Market Tote", category: "Tote bags", price: 1800, oldPrice: 2200, image: "", color: "#ded5f5", description: "A hand-crocheted everyday tote with comfortable woven handles." },
  { id: 3, name: "Sky Mini Crossbody", category: "Crossbody", price: 1450, image: "", color: "#cbeefe", description: "A light little companion for phone, keys and the essentials." },
  { id: 4, name: "Cloud Crochet Mat", category: "Home", price: 1200, oldPrice: 1500, image: "", color: "#f0eafd", description: "A soft textured accent, crocheted slowly and finished by hand." },
];
const money = (value: number) => `KSh ${value.toLocaleString("en-KE")}`;
const discount = (product: Product) => product.oldPrice ? Math.round((1 - product.price / product.oldPrice) * 100) : 0;

export default function Home() {
  const [view, setView] = useState<"shop" | "admin">("shop");
  const [products, setProducts] = useState(starterProducts);
  const [category, setCategory] = useState("All pieces");
  const [query, setQuery] = useState("");
  const [cart, setCart] = useState<CartItem[]>([]);
  const [cartOpen, setCartOpen] = useState(false);
  const [checkoutOpen, setCheckoutOpen] = useState(false);
  const [orderPlaced, setOrderPlaced] = useState(false);
  const [newProduct, setNewProduct] = useState({ name: "", category: "Bags", price: "", oldPrice: "" });
  const categories = ["All pieces", ...Array.from(new Set(products.map((item) => item.category)))];
  const filtered = products.filter((item) => (category === "All pieces" || item.category === category) && item.name.toLowerCase().includes(query.toLowerCase()));
  const count = cart.reduce((sum, item) => sum + item.quantity, 0);
  const subtotal = cart.reduce((sum, item) => sum + item.price * item.quantity, 0);
  const delivery = subtotal ? 250 : 0;
  function addToCart(product: Product) { setCart((items) => items.some((item) => item.id === product.id) ? items.map((item) => item.id === product.id ? { ...item, quantity: item.quantity + 1 } : item) : [...items, { ...product, quantity: 1 }]); setCartOpen(true); }
  function changeQuantity(id: number, amount: number) { setCart((items) => items.map((item) => item.id === id ? { ...item, quantity: item.quantity + amount } : item).filter((item) => item.quantity > 0)); }
  function addProduct(event: React.FormEvent) { event.preventDefault(); if (!newProduct.name || !newProduct.price) return; setProducts((items) => [{ id: Date.now(), name: newProduct.name, category: newProduct.category || "Bags", price: Number(newProduct.price), oldPrice: Number(newProduct.oldPrice) || undefined, image: "", color: "#d9e9f8", description: "A new handcrafted piece from Anner Creatives." }, ...items]); setNewProduct({ name: "", category: "Bags", price: "", oldPrice: "" }); }

  return <main className="min-h-screen overflow-x-hidden bg-[#fbfdff] text-[#17233b]">
    <header className="sticky top-0 z-40 border-b border-sky-100/80 bg-white/90 backdrop-blur-xl"><div className="mx-auto flex h-[76px] max-w-[1400px] items-center justify-between px-5 sm:px-8 lg:px-12"><button onClick={() => setView("shop")} className="flex items-center gap-3 text-left" aria-label="Go to shop"><Image src="/anner-logo.jpg" alt="Anner Creatives" width={54} height={54} className="h-12 w-12 rounded-2xl object-cover"/><div><p className="font-serif text-xl font-bold tracking-tight">Anner Creatives</p><p className="text-[11px] font-semibold uppercase tracking-[0.15em] text-sky-600">Made with heart</p></div></button><nav className="hidden items-center gap-8 text-sm font-semibold md:flex"><button onClick={() => setView("shop")} className={view === "shop" ? "text-sky-700" : "text-slate-500 hover:text-slate-900"}>Shop</button><a href="#story" onClick={() => setView("shop")} className="text-slate-500 hover:text-slate-900">Our story</a><button onClick={() => setView("admin")} className={view === "admin" ? "text-sky-700" : "text-slate-500 hover:text-slate-900"}>Admin</button></nav><Button onClick={() => setCartOpen(true)} variant="outline" className="relative h-11 rounded-full border-sky-200 bg-sky-50 px-4 text-sky-950"><ShoppingBag/> Cart {count > 0 && <span className="flex size-5 items-center justify-center rounded-full bg-[#e12b87] text-[11px] text-white">{count}</span>}</Button></div></header>
    {view === "shop" ? <Store products={filtered} categories={categories} category={category} setCategory={setCategory} query={query} setQuery={setQuery} addToCart={addToCart}/> : <AdminView products={products} setProducts={setProducts} newProduct={newProduct} setNewProduct={setNewProduct} addProduct={addProduct} onShop={() => setView("shop")}/>} 
    <footer className="mt-16 bg-[#13213d] px-5 py-12 text-white"><div className="mx-auto flex max-w-[1300px] flex-col justify-between gap-8 sm:flex-row sm:items-center"><div className="flex items-center gap-3"><Image src="/anner-logo.jpg" alt="" width={48} height={48} className="size-12 rounded-xl object-cover"/><div><p className="font-serif text-xl font-bold">Anner Creatives</p><p className="text-sm text-sky-200">Crafted with passion, designed with creativity.</p></div></div><p className="text-sm text-slate-400">Handmade with love in Kenya · © 2026</p></div></footer>
    <Cart open={cartOpen} setOpen={setCartOpen} items={cart} setItems={setCart} count={count} subtotal={subtotal} delivery={delivery} changeQuantity={changeQuantity} checkout={() => {setCartOpen(false);setCheckoutOpen(true)}}/>
    <Checkout open={checkoutOpen} setOpen={setCheckoutOpen} placed={orderPlaced} setPlaced={setOrderPlaced} total={subtotal+delivery}/>
  </main>;
}

function Store({products,categories,category,setCategory,query,setQuery,addToCart}:{products:Product[];categories:string[];category:string;setCategory:(v:string)=>void;query:string;setQuery:(v:string)=>void;addToCart:(p:Product)=>void}) { return <>
  <section className="relative mx-auto grid max-w-[1400px] gap-8 px-5 pb-16 pt-10 sm:px-8 lg:grid-cols-[1.05fr_.95fr] lg:px-12 lg:pt-14"><div className="relative z-10 flex flex-col justify-center py-8 lg:py-16"><div className="mb-7 inline-flex w-fit items-center gap-2 rounded-full border border-violet-200 bg-violet-50 px-4 py-2 text-sm font-semibold text-violet-700"><Sparkles className="size-4"/> Handmade in Kenya</div><h1 className="max-w-3xl font-serif text-5xl font-bold leading-[.98] tracking-[-0.045em] sm:text-6xl lg:text-[5.2rem]">Carry a little<br/><span className="brush-text">joy</span> everywhere.</h1><p className="mt-7 max-w-xl text-lg leading-8 text-slate-600">Thoughtfully handmade bags and crochet pieces, crafted with passion and designed with creativity.</p><div className="mt-9 flex flex-wrap gap-3"><Button asChild size="lg" className="h-12 rounded-full bg-[#1c89bd] px-7"><a href="#collection">Explore the collection <ArrowRight/></a></Button><Button asChild variant="outline" size="lg" className="h-12 rounded-full border-violet-200 px-7"><a href="#story">Meet the maker</a></Button></div><div className="mt-10 flex gap-8 border-t border-sky-100 pt-6 text-sm text-slate-600"><span><strong className="block text-xl text-slate-900">100%</strong> handmade</span><span><strong className="block text-xl text-slate-900">4.9/5</strong> happy customers</span><span><strong className="block text-xl text-slate-900">Kenya</strong> delivery</span></div></div><div className="hero-card relative min-h-[520px] overflow-hidden rounded-[2.4rem] bg-[#dff4ff] p-6 sm:p-9"><div className="absolute left-8 top-8 z-10 rounded-full bg-white/90 px-4 py-2 text-sm font-bold shadow-sm">Bestseller</div><Image src="/featured-backpack.jpg" alt="Sage green corduroy backpack" fill priority className="object-cover" sizes="(max-width: 1024px) 100vw, 50vw"/><div className="absolute inset-x-6 bottom-6 z-10 flex items-end justify-between rounded-[1.5rem] bg-white/90 p-5 shadow-xl backdrop-blur-md sm:inset-x-9 sm:bottom-9"><div><p className="text-sm font-medium text-slate-500">New arrival</p><h2 className="mt-1 font-serif text-2xl font-bold">Sage Corduroy Backpack</h2></div><p className="rounded-full bg-[#17233b] px-4 py-2 font-bold text-white">{money(2850)}</p></div></div></section>
  <section id="collection" className="mx-auto max-w-[1400px] px-5 py-14 sm:px-8 lg:px-12"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="eyebrow">Made for your everyday</p><h2 className="mt-2 font-serif text-4xl font-bold tracking-tight sm:text-5xl">Find your favourite</h2></div><div className="flex items-center gap-2 rounded-full border border-sky-100 bg-white px-4 shadow-sm"><Search className="size-4 text-slate-400"/><input value={query} onChange={(e)=>setQuery(e.target.value)} placeholder="Search pieces" className="h-11 w-44 bg-transparent text-sm outline-none"/></div></div><div className="mt-8 flex gap-2 overflow-x-auto pb-2">{categories.map((item)=><button key={item} onClick={()=>setCategory(item)} className={`whitespace-nowrap rounded-full px-5 py-2.5 text-sm font-semibold ${category===item?"bg-[#17233b] text-white":"border border-sky-100 bg-white text-slate-600"}`}>{item}</button>)}</div><div className="mt-9 grid gap-6 sm:grid-cols-2 lg:grid-cols-4">{products.map((product)=><ProductCard key={product.id} product={product} add={()=>addToCart(product)}/>)}</div></section>
  <section id="story" className="mx-auto my-16 grid max-w-[1300px] overflow-hidden rounded-[2.5rem] bg-[#ebe7fb] lg:grid-cols-2"><div className="p-8 sm:p-14 lg:p-20"><p className="eyebrow text-violet-700">Our little studio</p><h2 className="mt-3 font-serif text-4xl font-bold leading-tight sm:text-5xl">Every stitch carries a story.</h2><p className="mt-6 max-w-lg text-lg leading-8 text-slate-600">Anner Creatives turns colour, texture and imagination into useful pieces made to be loved. Each item is created in small batches, with the care only human hands can give.</p><div className="mt-8 flex items-center gap-3 font-semibold"><span className="flex size-10 items-center justify-center rounded-full bg-white"><Check className="size-5 text-violet-600"/></span> Crafted with passion, designed with creativity.</div></div><div className="logo-panel flex min-h-[390px] items-center justify-center bg-white/60 p-10"><Image src="/anner-logo.jpg" alt="Anner Creatives logo" width={420} height={420} className="w-full max-w-sm rounded-[2rem] mix-blend-multiply"/></div></section>
  </> }

function ProductCard({product,add}:{product:Product;add:()=>void}) { return <article className="product-card group rounded-[1.7rem] border border-sky-100 bg-white p-3 shadow-[0_18px_50px_rgba(65,132,169,.08)]"><div className="relative aspect-[4/4.5] overflow-hidden rounded-[1.25rem]" style={{background:product.color}}>{product.image?<Image src={product.image} alt={product.name} fill className="object-cover transition duration-500 group-hover:scale-105" sizes="(max-width: 640px) 100vw, 25vw"/>:<div className="flex h-full items-center justify-center"><ShoppingBag className="size-24 text-white/70" strokeWidth={1}/></div>}{discount(product)>0&&<span className="absolute left-3 top-3 rounded-full bg-[#e12b87] px-3 py-1 text-xs font-bold text-white">-{discount(product)}%</span>}<button aria-label="Save product" className="absolute right-3 top-3 flex size-9 items-center justify-center rounded-full bg-white/90 shadow-sm"><Heart className="size-4"/></button></div><div className="px-2 pb-2 pt-4"><p className="text-xs font-bold uppercase tracking-[.13em] text-sky-600">{product.category}</p><h3 className="mt-1 font-serif text-xl font-bold">{product.name}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-slate-500">{product.description}</p><div className="mt-4 flex items-center justify-between"><div><span className="font-bold">{money(product.price)}</span>{product.oldPrice&&<span className="ml-2 text-sm text-slate-400 line-through">{money(product.oldPrice)}</span>}</div><Button onClick={add} size="icon" className="rounded-full bg-sky-600" aria-label={`Add ${product.name} to cart`}><Plus/></Button></div></div></article> }

function Cart({open,setOpen,items,setItems,count,subtotal,delivery,changeQuantity,checkout}:{open:boolean;setOpen:(v:boolean)=>void;items:CartItem[];setItems:React.Dispatch<React.SetStateAction<CartItem[]>>;count:number;subtotal:number;delivery:number;changeQuantity:(id:number,n:number)=>void;checkout:()=>void}) { return <Sheet open={open} onOpenChange={setOpen}><SheetContent className="w-full border-sky-100 bg-[#fbfdff] sm:max-w-md"><SheetHeader className="border-b border-sky-100 p-6"><SheetTitle className="font-serif text-2xl">Your basket</SheetTitle><SheetDescription>{count?`${count} beautiful piece${count>1?"s":""} selected`:"Your next favourite piece belongs here."}</SheetDescription></SheetHeader><div className="flex-1 overflow-y-auto p-6">{items.length===0?<div className="flex h-full flex-col items-center justify-center text-center"><div className="flex size-20 items-center justify-center rounded-full bg-sky-100"><ShoppingBag className="size-9 text-sky-600"/></div><h3 className="mt-5 font-serif text-2xl font-bold">Your basket is empty</h3></div>:<div className="space-y-5">{items.map((item)=><div key={item.id} className="flex gap-4"><div className="relative size-20 shrink-0 overflow-hidden rounded-2xl" style={{background:item.color}}>{item.image?<Image src={item.image} alt="" fill className="object-cover"/>:<ShoppingBag className="m-6 text-white"/>}</div><div className="min-w-0 flex-1"><p className="truncate font-bold">{item.name}</p><p className="mt-1 text-sm text-slate-500">{money(item.price)}</p><div className="mt-2 flex items-center gap-3"><button onClick={()=>changeQuantity(item.id,-1)} className="flex size-7 items-center justify-center rounded-full border"><Minus className="size-3"/></button><span className="text-sm font-bold">{item.quantity}</span><button onClick={()=>changeQuantity(item.id,1)} className="flex size-7 items-center justify-center rounded-full border"><Plus className="size-3"/></button><button onClick={()=>setItems((xs)=>xs.filter((p)=>p.id!==item.id))} className="ml-auto text-slate-400 hover:text-rose-500"><Trash2 className="size-4"/></button></div></div></div>)}</div>}</div>{items.length>0&&<div className="border-t border-sky-100 bg-white p-6"><div className="space-y-2 text-sm"><div className="flex justify-between text-slate-500"><span>Subtotal</span><span>{money(subtotal)}</span></div><div className="flex justify-between text-slate-500"><span>Estimated delivery</span><span>{money(delivery)}</span></div><div className="flex justify-between pt-2 text-lg font-bold"><span>Total</span><span>{money(subtotal+delivery)}</span></div></div><Button onClick={checkout} className="mt-5 h-12 w-full rounded-full bg-[#1c89bd]">Continue to checkout <ArrowRight/></Button></div>}</SheetContent></Sheet> }

function Checkout({open,setOpen,placed,setPlaced,total}:{open:boolean;setOpen:(v:boolean)=>void;placed:boolean;setPlaced:(v:boolean)=>void;total:number}) { return <Dialog open={open} onOpenChange={(v)=>{setOpen(v);if(!v)setPlaced(false)}}><DialogContent className="max-h-[90vh] overflow-y-auto rounded-[1.5rem] border-sky-100 p-0 sm:max-w-2xl">{placed?<div className="p-10 text-center"><div className="mx-auto flex size-20 items-center justify-center rounded-full bg-emerald-100"><Check className="size-10 text-emerald-600"/></div><DialogTitle className="mt-6 font-serif text-3xl">Order received!</DialogTitle><p className="mx-auto mt-3 max-w-md text-slate-500">Your invoice is ready. Send <strong>{money(total)}</strong> to <strong>0712 345 678</strong> and use your name as the payment reference.</p><div className="mt-6 rounded-2xl bg-sky-50 p-5 text-left"><p className="text-xs font-bold uppercase tracking-widest text-sky-700">Invoice AN-{String(Date.now()).slice(-6)}</p><div className="mt-3 flex justify-between font-bold"><span>Total due</span><span>{money(total)}</span></div></div><Button onClick={()=>window.print()} variant="outline" className="mt-6 rounded-full">Print / save invoice</Button></div>:<><DialogHeader className="border-b border-sky-100 p-6 pr-12"><DialogTitle className="font-serif text-2xl">Delivery & payment</DialogTitle><DialogDescription>Tell us where to send your handmade pieces.</DialogDescription></DialogHeader><form onSubmit={(e)=>{e.preventDefault();setPlaced(true)}} className="grid gap-5 p-6 sm:grid-cols-2"><Field label="Full name"><Input required placeholder="Your name"/></Field><Field label="Phone number"><Input required type="tel" placeholder="07•• ••• •••"/></Field><Field label="County / town"><Input required placeholder="e.g. Nairobi"/></Field><Field label="Delivery area"><Input required placeholder="Estate or landmark"/></Field><div className="sm:col-span-2"><Label>Delivery notes</Label><textarea className="mt-2 min-h-20 w-full rounded-xl border border-input bg-transparent p-3 text-sm outline-none" placeholder="Any helpful directions?"/></div><div className="sm:col-span-2 rounded-2xl border border-sky-200 bg-sky-50 p-5"><div className="flex gap-3"><div className="flex size-10 shrink-0 items-center justify-center rounded-full bg-sky-600 font-bold text-white">M</div><div><p className="font-bold">M-PESA Send Money</p><p className="mt-1 text-sm text-slate-600">After placing your order, send the total to <strong>0712 345 678</strong>. We’ll confirm by phone.</p></div></div></div><div className="sm:col-span-2 flex items-center justify-between border-t pt-5"><div><p className="text-sm text-slate-500">Total due</p><p className="text-xl font-bold">{money(total)}</p></div><Button type="submit" className="h-11 rounded-full bg-[#1c89bd] px-6">Place order</Button></div></form></>}</DialogContent></Dialog> }

function Field({label,children}:{label:string;children:React.ReactNode}) { return <div><Label>{label}</Label><div className="mt-2">{children}</div></div> }
const sum = 0;
function AdminView({products,setProducts,newProduct,setNewProduct,addProduct,onShop}:{products:Product[];setProducts:React.Dispatch<React.SetStateAction<Product[]>>;newProduct:{name:string;category:string;price:string;oldPrice:string};setNewProduct:React.Dispatch<React.SetStateAction<{name:string;category:string;price:string;oldPrice:string}>>;addProduct:(e:React.FormEvent)=>void;onShop:()=>void}) { const stockValue=useMemo(()=>products.reduce((sum,p)=>sum+p.price,sum),[products]); return <section className="mx-auto min-h-[75vh] max-w-[1400px] px-5 py-10 sm:px-8 lg:px-12"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="eyebrow">Store workspace</p><h1 className="mt-2 font-serif text-4xl font-bold">Good evening, Anner.</h1><p className="mt-2 text-slate-500">Manage what your customers see in the shop.</p></div><Button onClick={onShop} variant="outline" className="rounded-full">View storefront <ArrowRight/></Button></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><Stat label="Products" value={String(products.length)} note="Visible in the shop"/><Stat label="Catalog value" value={money(stockValue)} note="Based on current prices"/><Stat label="Orders" value="12" note="3 awaiting payment"/></div><div className="mt-8 grid gap-7 lg:grid-cols-[1fr_360px]"><div className="overflow-hidden rounded-[1.5rem] border border-sky-100 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-sky-100 p-5"><div><h2 className="font-serif text-2xl font-bold">Products</h2><p className="text-sm text-slate-500">Prices and offers at a glance</p></div><Package className="text-sky-600"/></div><Table><TableHeader><TableRow><TableHead>Product</TableHead><TableHead>Category</TableHead><TableHead>Price</TableHead><TableHead>Offer</TableHead><TableHead/></TableRow></TableHeader><TableBody>{products.map((p)=><TableRow key={p.id}><TableCell className="font-semibold">{p.name}</TableCell><TableCell className="text-slate-500">{p.category}</TableCell><TableCell>{money(p.price)}</TableCell><TableCell>{discount(p)?`${discount(p)}% off`:"—"}</TableCell><TableCell><button onClick={()=>setProducts((xs)=>xs.filter((x)=>x.id!==p.id))} className="text-slate-400 hover:text-rose-500" aria-label={`Delete ${p.name}`}><Trash2 className="size-4"/></button></TableCell></TableRow>)}</TableBody></Table></div><form onSubmit={addProduct} className="h-fit rounded-[1.5rem] border border-violet-100 bg-[#f5f2ff] p-6"><h2 className="font-serif text-2xl font-bold">Add a product</h2><p className="mt-1 text-sm text-slate-500">Create a fresh listing for the shop.</p><div className="mt-6 space-y-4"><Field label="Product name"><Input value={newProduct.name} onChange={(e)=>setNewProduct({...newProduct,name:e.target.value})} required placeholder="e.g. Ocean tote"/></Field><Field label="Category"><Input value={newProduct.category} onChange={(e)=>setNewProduct({...newProduct,category:e.target.value})}/></Field><div className="grid grid-cols-2 gap-3"><Field label="Current price"><Input value={newProduct.price} onChange={(e)=>setNewProduct({...newProduct,price:e.target.value})} required min="1" type="number"/></Field><Field label="Before price"><Input value={newProduct.oldPrice} onChange={(e)=>setNewProduct({...newProduct,oldPrice:e.target.value})} min="1" type="number"/></Field></div><Field label="Product photo"><Input type="file" accept="image/*" className="bg-white"/></Field><Button type="submit" className="h-11 w-full rounded-full bg-violet-600"><Plus/> Add to collection</Button></div></form></div></section> }
function Stat({label,value,note}:{label:string;value:string;note:string}) { return <div className="rounded-[1.4rem] border border-sky-100 bg-white p-6 shadow-sm"><p className="text-sm font-semibold text-slate-500">{label}</p><p className="mt-2 font-serif text-3xl font-bold">{value}</p><p className="mt-2 text-xs text-sky-700">{note}</p></div> }
